# Telemetry Agent - Vendor Drop

This repository contains the upstream vendor-supplied distribution archive for the enterprise telemetry agent. 

## Archive Details

* **File:** `telemetry-agent-v2.4.1.cpio`
* **Contents:** Raw, unmodified source code and build assets for the telemetry daemon.

## Extraction

To extract the contents of the archive, run the following command:

```bash
cpio -idv < telemetry-agent-v2.4.1.cpio
